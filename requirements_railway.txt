# Railway-optimized requirements
# Minimal dependencies for reliable deployment

Flask>=2.3.0
Werkzeug>=2.3.0
requests>=2.31.0
python-dotenv>=1.0.0

# Data processing (let Railway choose compatible versions)
pandas>=1.5.0

# Optional document processing (install if available)
PyMuPDF>=1.20.0
python-docx>=0.8.0
Pillow>=9.0.0
